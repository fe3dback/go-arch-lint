version: 4

settings:
  structTags:
    allowed: [ db, bd,
      another,
      json
    ]

components:
  main:                { in: app }
  models:              { in: models }

dependencies:
  main:
    mayDependOn:
      - models